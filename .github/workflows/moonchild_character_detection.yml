
on:
  workflow_dispatch: {}
  
name: Moonchild Character Detection

on:
  push:
    paths:
      - 'main/registry/characters/**'   # adjust if your path differs
  workflow_dispatch: {}

jobs:
  autodetect:
    uses: ./.github/workflows/_moonchild_autodetect_reusable.yml
    with:
      reason: "character folder changed"